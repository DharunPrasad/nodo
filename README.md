Instructions to new developers:

1) Download or clone the repository.
2) Open the folder in any code editor and run "npm install" to install the dependencies.
3) Run the command "npm start".
4) Go on and access the app @ localhost:8000 using any browser.
